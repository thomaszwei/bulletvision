# ─────────────────────────────────────────────────────────────────────────────
# BulletVision Backend — Python dependencies
# Pinned for reproducibility on Raspberry Pi (arm64 / armv7l).
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9          # file uploads

# Database
sqlalchemy==2.0.30
alembic==1.13.1
aiosqlite==0.20.0                # async SQLite driver

# Validation / settings
pydantic==2.7.3
pydantic-settings==2.3.4

# Computer vision
# opencv-python-headless is smaller; no GUI windows needed on Pi server.
opencv-python-headless==4.9.0.80
numpy==1.26.4

# Image I/O
Pillow==10.3.0

# Utilities
python-dateutil==2.9.0
httpx==0.27.0                    # async HTTP client (for health checks / demo)
anyio==4.4.0
psutil==5.9.8                    # system stats: CPU, memory, temperature

# picamera2 is installed system-wide on Raspberry Pi OS (not via pip).
# We do NOT include it here to keep the image cross-platform.
# The camera service falls back gracefully when picamera2 is unavailable (demo mode).
